{
    "builds": [
        {
        "src": "api/main.py",
        "use": "@vercel/node"
        }
    ],

    "routes": [
        {
        "src": "/(.*)",
        "dest": "api/main.py"
        }
    ]
}